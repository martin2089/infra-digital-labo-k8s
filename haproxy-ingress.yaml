apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: haproxy-ingress
spec:
  rules:
  - http:
      paths:
      - path: /prueba
        backend:
          serviceName: nginx
          servicePort: 80
